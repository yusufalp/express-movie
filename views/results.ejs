<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include('head') %>
		<title>Search Results</title>
	</head>
</html>
<body>
	<h1>Results</h1>
	<p>Results for "<%= searchedFor %>"</p>
	<div class="container">
		<% for (result of movieSearchResults) { %>
		<div class="row">
			<a href="movies/<%= result.id %>">
				<img
					src="<%= imageBaseUrl + result.poster_path %>"
					alt="<%= result.title %>"
				/>
			</a>
			<p><%= result.title %></p>
			<p><%= result.overview %></p>
		</div>
		<% } %>
	</div>
</body>
